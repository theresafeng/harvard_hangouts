<%= form_for(@hangout) do |f| %>
  <% if @hangout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hangout.errors.count, "error") %> prohibited this hangout from being saved:</h2>

      <ul>
      <% @hangout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="span3">
  <div class="field">
    <%= f.select :event, [['Study', 'Study'],['Session', 'Session'],['Party', 'Party']]%>
  </div>
  
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
</div>
<div class="span5 offset">
  <div class="field">
    <%= f.label :start_date_time %><br />
    <%= f.time_select :start_date_time %>
  </div>
  <div class="field">
    <%= f.label :end_date_time %><br />
    <%= f.time_select :end_date_time %>
  </div>
</br>
    <button class="btn btn-primary" type="submit">Submit</button>
    <a class="btn" href="/hangouts">Cancel</a>
    
  </div>
</div>
<% end %>
